/** @type {import('next').NextConfig} */
const nextConfig = {
    // next.config.mjs

    images: {
      domains: ['cdn.buymeacoffee.com'], // Add other domains if needed
    },

  
};

export default nextConfig;
